[project]
name = "interview-intelligence"
version = "0.1.0"
description = "Local-first interview transcription and feedback intelligence platform."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "fastapi>=0.141.1",
    "mlx-whisper>=0.4.3",
    "openai>=3.3.1",
    "pyannote-audio>=4.0.7",
    "pydantic>=2.0,<3.0",
    "pydantic-settings>=2.0,<3.0",
    "python-multipart>=0.0.32",
    "structlog>=24.0,<26.0",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
interview-review-benchmark = "interview_intelligence.review.benchmark:main"

[dependency-groups]
dev = [
    "pytest>=8.0,<9.0",
    "pytest-cov>=5.0,<7.0",
    "ruff>=0.6,<1.0",
    "mypy>=1.10,<2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/interview_intelligence"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["interview_intelligence"]
mypy_path = "src"
